# Build the application
- Assuming you've cloned the project files, run `npm install` from the root of the project to install necessary dependencies.
- Then `npm run build`
# Start a local server
- `npm run dev`
